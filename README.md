# 横浜市立大学医学部硬式テニス部 OBOG会 Web Site

GitHub Pages 用の現行ファイル一式です。

## 試合結果の更新
`results.xlsx` を更新して同名で上書きします。
`results.html` が一覧、`match.html` が FULL RESULT を表示します。

## 重要
- `style.css` と `site.js` はルートに置く
- `top-court.webp` もルートに置く
- `members-1.html` ～ `members-6.html` は削除しない
- 旧版の `results_data.xlsx`、`match_v2.html` などは不要
